{
  "name": "@universal-canvas/engine",
  "version": "0.1.9",
  "description": "A universal canvas engine with adapters for Fabric, Skyline, etc.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && node scripts/build-umd.js",
    "prepare": "npm run build",
    "dev": "concurrently \"tsc --watch\" \"live-server --port=5175 --host=localhost --mount=/:examples\""
  },
  "files": [
    "dist"
  ],
  "peerDependencies": {
    "fabric": "^6.9.0"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^29.0.0",
    "@rollup/plugin-node-resolve": "^16.0.3",
    "@rollup/plugin-typescript": "^12.3.0",
    "concurrently": "^7.6.0",
    "live-server": "^1.2.2",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@universal-canvas/schema": "^1.0.0"
  }
}
