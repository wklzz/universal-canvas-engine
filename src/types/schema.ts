// 从@universal-canvas/schema导入类型定义
export type { 
  CanvasSchema, 
  CanvasElement, 
  CanvasMetadata,
  CanvasDocument,
  CanvasProperties
} from '@universal-canvas/schema';

export type { 
  ElementType,
  RectangleElement,
  CircleElement,
  TriangleElement,
  LineElement,
  TextElement,
  ImageElement,
  GroupElement,
  PathElement
} from '@universal-canvas/schema/dist/types/elements';